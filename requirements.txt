# Core simulation/runtime dependencies
mujoco>=2.3.3
numpy>=2.0,<3
python-dotenv>=1.0.0
pin>=2.6.0
robot_descriptions>=1.0.0
pytest>=8.0
ompl>=1.6.0
pandas>=2.2.0
matplotlib>=3.8.0

# OMPL Python bindings are required for the OMPL-only task scripts and planner.
# On Linux/WSL this may work:
#   pip install ompl
# Verify with:
#   python -c "from ompl import base, geometric; print('ompl ok')"
#
# If pip ompl is unavailable for your Python/OS combo, install OMPL separately
# from system packages/source and ensure the Python bindings are importable.
